[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
markers =
    no_auth_bypass: do not override JWT auth dependencies in this test (use for tests that exercise auth semantics themselves)
